.introWrap
  display flex
  flex-direction column
  align-items center

.introText
  font-size 18px
  color var(--contrast-color)
  text-align center
  padding 8px 0 28px

.exactDeathDateBlock
  display flex
  justify-content center
  text-align center
  padding 32px 0 10px

  button
    p
      font-size 10px
      font-weight 400
      opacity 0.45

.nonOpacity
  button
    p
      opacity 1