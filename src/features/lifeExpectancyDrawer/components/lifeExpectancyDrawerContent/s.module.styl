.introWrap
  display flex
  flex-direction column
  align-items center

.introText
  font-size 16px
  color var(--contrast-color)
  text-align center
  padding 8px 0 28px

.exactDeathDateBlock
  display flex
  justify-content center
  text-align center
  padding 32px 0 0

  button
    filter brightness(0.5)

    p
      font-size 10px
      font-weight 400

.nonOpacity
  button
    filter none

    p
      opacity 1