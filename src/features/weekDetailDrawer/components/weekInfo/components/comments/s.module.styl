.comments
  margin-bottom 15px

.commentsLabel
  color var(--secondary-color)
  font-size 14px
  font-weight 500
  margin-bottom 5px

.commentsText
  color var(--text-color)
  font-size 14px
  line-height 1.5
  font-style italic
