.wrapper
  padding 20px 10px

.title
  display flex
  gap 12px

.year
  color var(--secondary-color)

.month
  color var(--brand-g1-color)

.secondColor
  color var(--brand-g2-color)

.dates
  color var(--secondary-color)