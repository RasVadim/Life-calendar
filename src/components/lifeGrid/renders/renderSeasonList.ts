import { Container } from 'pixi.js';

import { TLifeGridState } from '../types';

/**
 * Renders the seasons-mode grid (row = calendar season).
 * TODO: not implemented yet — returns null so renderLife skips the scroll container.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const renderSeasonList = (state: TLifeGridState): Container | null => {
  return null;
};
