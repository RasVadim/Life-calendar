.wrapper
  position relative
  display inline-block
  min-width 200px
  user-select none
  width 80%
  max-width 300px

.selected
  display flex
  align-items center
  justify-content space-between
  padding 8px 16px
  border 1px solid var(--border-color)
  border-radius 40px
  background-color var(--background-front-color)
  font-size 14px
  line-height 1.5
  color var(--text-color)
  cursor pointer
  transition all 0.2s ease

  &:hover
    border-color var(--brand-g2-color)
    background-color var(--block-hover-bg)

.wrapper.open .selected
  border-color var(--brand-g2-color)

.wrapper.disabled .selected
  cursor not-allowed
  opacity 0.7
  color var(--disabled-color)

  .arrow
    border-top-color var(--disabled-color)

.arrow
  width 10px
  height 4px
  margin-left 8px
  background linear-gradient(to top right, var(--brand-g1-color), var(--brand-g2-color))
  clip-path polygon(0 0, 100% 0, 50% 100%)
  transition transform 0.2s ease

.wrapper.open .arrow
  transform rotate(90deg)

.options
  position absolute
  top 100%
  left 0
  right 0
  margin-top 4px
  padding 4px 0
  background var(--background-front-color)
  border-radius 20px
  box-shadow 0 1px 4px var(--background-front-color, 0.15)
  z-index 1000
  max-height 240px
  overflow-y auto

  // Scrollbar styles
  &::-webkit-scrollbar
    width 2px !important

  &::-webkit-scrollbar-track
    background transparent
    border-radius 4px
    width 2px !important

  &::-webkit-scrollbar-thumb
    background var(--scrollbar-color)
    border-radius 4px
    width 2px !important

    &:hover
      background linear-gradient(to top right, var(--brand-g1-color), var(--brand-g2-color))

  // Firefox
  scrollbar-width thin
  scrollbar-color var(--scrollbar-color) transparent

.option
  padding 8px 12px
  cursor pointer
  transition background-color 0.2s ease
  color var(--text-color)

  &:hover
    background-color var(--block-hover-bg)

  &.selected
    background linear-gradient(to top right, var(--brand-g1-color), var(--brand-g2-color))
    opacity 0.8
    color var(--opposite-contrast-color)
    border none
    box-shadow none
    border-radius 0